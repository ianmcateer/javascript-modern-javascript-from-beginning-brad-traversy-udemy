const num1 = 100
const num2 = 50
let val


// simple math with numbers
val = num1 + num2
val = num1 * num2
val = num1 - num2
val = num1 % 60

// msth object- it has properties and emthods
// property is an attribute a method is a funciton isndie of an object 
val = Math.PI
val = Math.E
val = Math.round(2.5)
val = Math.ceil(2.5)
val = Math.floor(2.5)
val = Math.sqrt(64)
val = Math.abs(-90)
val = Math.pow(8, 4)
val = Math.min(2,4,5,677,2343,1,-9)
val = Math.max(2, 4, 5, 677, 2343, 1, -9)
val = Math.random()
val = Math.floor(Math.random() * 20 + 1)
console.log(val)